# Required
DISCORD_TOKEN=your-discord-bot-token
OWNER_ID=your-discord-user-id
VAULT_PATH=/path/to/your/obsidian/vault

# Claude Provider (default: cli)
# cli = Claude Code CLI (needs Claude Code installed + subscription)
# api = Anthropic API (needs API key, chat only — no vault/MCP access)
CLAUDE_PROVIDER=cli

# For CLI provider (optional)
CLAUDE_PATH=claude
CLAUDE_MODEL=sonnet
MAX_BUDGET_USD=1.00

# For API provider only
# CLAUDE_API_KEY=sk-ant-...

# Other
DOWNLOAD_DIR=/tmp/second-brain-kit-files
LOG_LEVEL=info
